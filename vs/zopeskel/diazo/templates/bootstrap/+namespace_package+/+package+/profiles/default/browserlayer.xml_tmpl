<?xml version="1.0"?>
<!-- Registers the package-specific browser layer,
     so that it will be activated on install -->
<layers>
    <layer name="${namespace_package}.${package}"
      interface="${namespace_package}.${package}.interfaces.ICustomTheme" />
</layers>

